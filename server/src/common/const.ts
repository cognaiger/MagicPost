export enum Role {
    Boss = 'Boss',
    EPManager = 'EPManager',
    EPOperator = 'EPOperator',
    CPManager = 'CPManager',
    CPStaff = 'CPStaff',
    Client = 'Client'
}

export enum PointType {
    TPoint = 'TPoint',
    CPoint = 'CPoint'
}